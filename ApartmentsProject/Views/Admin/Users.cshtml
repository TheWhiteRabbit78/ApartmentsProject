@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>

@{
    ViewData["Title"] = "Upravljanje korisnicima";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Upravljanje korisnicima</h2>
        <div>
            <a asp-action="CreateUser" class="btn btn-primary">Stvori novog korisnika</a>
            <a asp-action="Index" class="btn btn-secondary">Povratak na stanove</a>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Email</th>
                            <th>Korisničko ime</th>
                            <th>Email potvrđen</th>
                            <th>Akcije</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@user.Email</td>
                                <td>@user.UserName</td>
                                <td>
                                    <span class="badge @(user.EmailConfirmed ? "bg-success" : "bg-warning")">
                                        @(user.EmailConfirmed ? "Da" : "Ne")
                                    </span>
                                </td>
                                <td>
                                    <form asp-action="DeleteUser" method="post" class="d-inline">
                                        <input type="hidden" name="id" value="@user.Id" />
                                        <button type="submit" class="btn btn-sm btn-outline-danger"
                                                onclick="return confirm('Obrisati ovog korisnika?')">
                                            Obriši
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>